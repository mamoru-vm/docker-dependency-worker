# docker-dependency-worker
Node DDP client script and container to manage status of decencies independently of Meteor Application server.

- Pulled and run automatically by Meteor App server on startup
